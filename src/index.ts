import {
    randomNum,
    randomHexColor,

    is,
    isObject,
    isArray,
    isFunction,
    isEmptyObject,

    shuffleArray,

    debounce,
} from "./util";

export {
    randomNum,
    randomHexColor,

    is,
    isObject,
    isArray,
    isFunction,
    isEmptyObject,

    shuffleArray,

    debounce,
}

const rMiniUtil = {
    randomNum,
    randomHexColor,

    is,
    isObject,
    isArray,
    isFunction,
    isEmptyObject,

    shuffleArray,

    debounce,
}

export default rMiniUtil

